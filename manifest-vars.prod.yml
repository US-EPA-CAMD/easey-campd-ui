instances: 2
environment: production
host: campd.epa.gov
apiHost: api.epa.gov/easey
googleAnalyticsEnabled: "true"
command: NODE_OPTIONS="--max_old_space_size=$(( $MEMORY_AVAILABLE * 75 / 100 ))" serve -s build
